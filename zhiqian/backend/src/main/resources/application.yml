server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: zhiqian-backend
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/zhiqian}
    username: ${SPRING_DATASOURCE_USERNAME:zhiqian}
    password: ${SPRING_DATASOURCE_PASSWORD:zhiqian123}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  jwt:
    secret: ${APP_JWT_SECRET:change-me-please-change-me-please-32bytes-min}
    ttl-minutes: ${APP_JWT_TTL_MINUTES:720}
  rag:
    base-url: ${APP_RAG_BASE_URL:http://localhost:8001}

logging:
  level:
    root: INFO
    com.zhiqian: DEBUG
