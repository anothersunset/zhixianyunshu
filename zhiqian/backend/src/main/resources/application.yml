server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: zhiqian-backend
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/zhiqian}
    username: ${SPRING_DATASOURCE_USERNAME:zhiqian}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  jwt:
    secret: ${APP_JWT_SECRET}
    ttl-minutes: ${APP_JWT_TTL_MINUTES:60}
  admin:
    password: ${APP_ADMIN_PASSWORD}
  rag:
    base-url: ${APP_RAG_BASE_URL:http://localhost:8001}
  # ===== v2-step-02 新增：LLM 接入配置 =====
  # 默认指向 DeepSeek-V3.1，使用 OpenAI 兼容协议。
  # api-key 为空时后端会启用 MockLlmClient（仅回显），不阻塞启动。
  llm:
    provider: ${LLM_PROVIDER:deepseek}
    api-key: ${LLM_API_KEY:}
    base-url: ${LLM_BASE_URL:https://api.deepseek.com/v1}
    chat-model: ${LLM_CHAT_MODEL:deepseek-chat}
    reasoner-model: ${LLM_REASONER_MODEL:deepseek-reasoner}
    temperature: ${LLM_TEMPERATURE:0.2}
    max-tokens: ${LLM_MAX_TOKENS:2048}
    timeout-seconds: ${LLM_TIMEOUT_SECONDS:60}
  # ===== v2-step-08 新增：Langfuse 后端全链观测 =====
  # public-key/secret-key 留空即 disabled，行为完全不变，不影响功能。
  # 配上后，task.migration root trace + 6 stage span + 每次 LLM 调用 generation 都会上报。
  langfuse:
    enabled: ${LANGFUSE_ENABLED:true}
    host: ${LANGFUSE_HOST:https://cloud.langfuse.com}
    public-key: ${LANGFUSE_PUBLIC_KEY:}
    secret-key: ${LANGFUSE_SECRET_KEY:}

logging:
  level:
    root: INFO
    com.zhiqian: DEBUG
