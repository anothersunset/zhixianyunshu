# RAG test dependencies.
pytest==8.3.2
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.28.1

# RAGAS / LangChain tests only. test_ragas.py skips when API keys are absent.
ragas==0.2.0
langchain==0.3.0
langchain-core==0.3.0
langchain-openai==0.2.0
langchain-huggingface==0.1.0
datasets==2.20.0
