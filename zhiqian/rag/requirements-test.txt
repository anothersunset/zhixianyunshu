# v2-step-11: 测试依赖。与产品镜像隔离, 避免多装 langchain/datasets 这些重包。
# 装上这份后才能 跑 make test-fast / test-ragas。

# 基础
 pytest==8.3.2
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2          # FastAPI TestClient 依赖

# RAGAS 与 LangChain (仅 test_ragas.py 需)
ragas==0.2.0
langchain==0.3.0
langchain-core==0.3.0
langchain-openai==0.2.0
langchain-huggingface==0.1.0
datasets==2.20.0
