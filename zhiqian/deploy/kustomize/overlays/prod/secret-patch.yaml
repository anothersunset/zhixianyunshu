# v2-step-18: prod secret 示例 patch
# 实际生产请用 External Secrets Operator / Sealed Secrets / Vault
# 此文件仅示例格式, 请勿将真 secret 提交 git
apiVersion: v1
kind: Secret
metadata:
  name: zhiqian-backend-secret
type: Opaque
stringData:
  # 生产取消下面注释, 填真 secret (或从 Vault 同步)
  # jwt-secret: "PRODUCTION-JWT-SECRET-AT-LEAST-32-CHARS-LONG"
  # deepseek-api-key: "sk-xxxxxxxxxxxxx"
  # langfuse-public-key: "pk-lf-xxxxx"
  # langfuse-secret-key: "sk-lf-xxxxx"
  # postgres-password: "strong-random-password"
  jwt-secret: "PLACEHOLDER-REPLACE-WITH-REAL-32-CHAR-MIN-RANDOM"
